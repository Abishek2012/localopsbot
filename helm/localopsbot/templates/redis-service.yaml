apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-redis

spec:
  selector:
    app: localopsbot
    component: redis

  ports:
    - name: redis
      port: 6379
      targetPort: redis

  type: ClusterIP