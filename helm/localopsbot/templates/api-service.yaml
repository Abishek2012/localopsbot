apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-api

spec:
  selector:
    app: localopsbot
    component: api

  ports:
    - name: http
      port: {{ .Values.api.service.port }}
      targetPort: http

  type: {{ .Values.api.service.type }}